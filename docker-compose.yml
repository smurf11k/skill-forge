services:
  postgres:
    image: postgres:16
    container_name: skillforge_db
    restart: unless-stopped
    environment:
    - ./backend/.env
    ports:
      - "5432:5432"
    volumes:
      - skillforge_pgdata:/var/lib/postgresql/data

volumes:
  skillforge_pgdata:
